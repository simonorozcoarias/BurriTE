process {
    executor = 'slurm'
    queue = params.queue
    clusterOptions = params.cluster_options ?: ''

    // GraffiTE is itself a Nextflow pipeline. Its launcher must stay on the
    // submission node; the nested GraffiTE workflow submits its own jobs.
    withLabel: orchestrator {
        executor = 'local'
    }
}
