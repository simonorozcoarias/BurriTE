Copy the two scripts used by the validated BurriTE v3 installation into this
directory:

- `build_dictionary.pl`
- `one_code_to_find_them_all_but_sanely.pl`

Alternatively, point the workflow to the existing installation:

```bash
--onecode_dir /path/to/BurriTE/tools/OneCodeToFindThemAll
```

The second script is the BurriTE-compatible â€œbut sanelyâ€ variant, not the
unmodified upstream script.